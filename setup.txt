# API Project

## Setup

Uses the following boilerplate files:

1. setupExpress.js
2. setupHBS.js
3. MongoUtil.js
4. User authentication: `passport/setup.js` and `models/UserModel`

And we need to have `.env` file with the following settings:

* `MONGO_URL`